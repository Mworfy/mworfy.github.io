Font Files Required:

Please add the following font files to this directory:

1. TacticRoundExtraExtendedUltra
   - TacticRoundExtraExtendedUltra.woff2 (preferred)
   - TacticRoundExtraExtendedUltra.woff (fallback)
   - TacticRoundExtraExtendedUltra.ttf (fallback)

2. SkapaBold
   - SkapaBold.woff2 (preferred)
   - SkapaBold.woff (fallback)
   - SkapaBold.ttf (fallback)

3. AsketLight
   - AsketLight.woff2 (preferred)
   - AsketLight.woff (fallback)
   - AsketLight.ttf (fallback)

These fonts are commercial fonts that can be purchased from MyFonts.com.
Once you have the font files, place them in this directory with the exact names above.
